prime算法已更改


为了让每个worker独立写读快照
这个worker加入了新参数，运行时需要输入参数worker id，例如
python worker.py 1
python worker.py 2
python worker.py 3
python worker.py 4

！！！崩溃的worker可以从快照恢复，但是不知道为什么，恢复后的worker完成全部任务后，并不能把结果发给coordinator
如果没有崩溃模拟，正常运行的话，输出结果正常

text1000.txt里是用numbers_generators随机生成的1000个数,如果正确运行的话应该结果有71个素数

111.py是查看快照用的

ack机制（coordinator发一次ok，worker收到后关闭）是ai建议我加的，但是好像对功能没有什么提升



崩溃模拟是再worker代码段里，在75行处可以设置崩溃概率，断掉后要手动重启

任务跟踪+心跳检测基本完成，与snapshots的coordinator—worker版本对接一下就行，之后再实践测试运行存在的问题

update11/10: 我用gpt结合了我们两个version的，在xxx_mix中
